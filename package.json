{
  "name": "redis-example",
  "version": "1.0.0",
  "description": "Simple example app for connecting to a Redis instance through docker",
  "main": "tasks.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "redis": "^4.6.7"
  }
}
